#include<stdio.h>
/* - main  prints all possible combinations of two two-digit numbers.
 * Description  prints all possible combinations of two two-digit numbers.
 * Return : 0
 */
int main(void)
{
int ff;
int fs;
int sf;
int ss;
for (ff = 48 ; ff <= 57 ; ff++)
{
	for ( fs = 48 ; fs <= 57 ; fs++ )
	{
		
		for ( sf = ff ; sf <= 57 ; sf++ )
		{
			for ( ss=fs+1 ; ss <= 57 ; ss++)
			{
				 putchar(ff);
                putchar(fs);
				putchar(' ');
				putchar(sf);
				putchar(ss);
				putchar(',');
				putchar(' ');
			}
		}
	}
	
}
putchar('\n');
return(0);
}				
